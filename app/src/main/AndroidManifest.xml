<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wit.festivalapp">

    <application
        android:name="org.wit.placemark.main.mainApp"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".location.locationScreen"/>
        <activity android:name=".image.imageScreen" />
        <activity android:name=".timetable.timetableScreen" />
        <activity android:name=".artists.artistScreen" />
        <activity android:name=".artists.artistDetails" />
        <activity android:name=".home.homeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>