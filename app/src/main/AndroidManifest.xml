<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wit.festivalapp">

    <uses-permission android:name="android.permission.INTERNET"/>

     <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
         <activity android:name=".timetable.TimetableDetails"/>
         <activity android:name=".artists.UpdateArtist" />
        <activity android:name=".timetable.timetableScreen" />
        <activity android:name=".artists.ArtistScreen" />
        <activity android:name=".artists.ArtistDetails" />
        <activity android:name=".artists.store.ArtistModel" />
        <activity android:name=".artists.AddArtist" />
        <activity android:name=".home.HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>